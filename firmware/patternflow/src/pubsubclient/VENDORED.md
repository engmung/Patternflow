Nick O'Leary's PubSubClient (MQTT 3.1.1), vendored so Arduino IDE and
PlatformIO both compile it from `src/`. Upstream:
https://github.com/knolleary/pubsubclient

Brought in by Simone Majocchi (@SimonePDA) along with the MQTT sidechannel
in `features/mqtt/core_mqtt.h`.
